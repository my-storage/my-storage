# ms-profile

#### General

Create a new file `.env` based on `.env.example` file.

```bash
cp .env.example .env
```

#### Running

Run service in Development

```bash
go run src/main.go
```

#### Testing

Run service in Development

```bash
go test ./...
```
